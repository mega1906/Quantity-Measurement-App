{
  "name": "quantitymeasurementapp",
  "version": "1.0.0",
  "description": "This project currently includes a static quantity measurement UI and a seeded `db.json` file for JSON Server.",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
